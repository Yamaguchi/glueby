class CreateTxReissue < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :reissues<%= table_options %> do |t|
      t.string     :txid, null: true
      t.integer    :status, null: false
      t.bigint     :amount, null: false
      t.string     :receiver_wallet_id, null: false
      t.belongs_to :token
      t.timestamps
    end

    add_index :reissues, [:txid], unique: true
  end
end
