class CreateTxTransfer < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :transfers<%= table_options %> do |t|
      t.string     :txid, null: true
      t.integer    :status, null: false
      t.bigint     :amount, null: false
      t.string     :sender_wallet_id, null: false
      t.string     :receiver_wallet_id, null: false
      t.belongs_to :token
      t.timestamps
    end

    add_index :transfers, [:txid], unique: true
  end
end
