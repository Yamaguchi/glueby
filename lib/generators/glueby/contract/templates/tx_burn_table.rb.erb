class CreateTxBurn < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :burns<%= table_options %> do |t|
      t.string     :txid, null: true
      t.integer    :status, null: false
      t.bigint     :amount, null: false
      t.string     :sender_wallet_id, null: false
      t.belongs_to :token
      t.timestamps
    end

    add_index :burns, [:txid], unique: true
  end
end
