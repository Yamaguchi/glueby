class CreateTxPayment < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :payments<%= table_options %> do |t|
      t.string     :txid, null: true
      t.integer    :status, null: false
      t.bigint     :amount, null: false
      t.string     :sender_wallet_id, null: false
      t.string     :receiver_wallet_id, null: false
      t.timestamps
    end

    add_index :payments, [:txid], unique: true
  end
end
